# yaml-language-server: $schema=../../spore-os-protocol/versions/v1/spore.manifest.v1.schema.yaml

id: dev.sporeos.spore
name: Spore
version: 0.1.0
schema: SPORE/v1d0
description: One-off command tool. Sends a single command to the hub and prints the response.
app: /usr/local/bin/spore
api: []
